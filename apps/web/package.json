{
  "name": "@kleavox/web-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit --checkers 1"
  },
  "dependencies": {
    "@kleavox/config": "workspace:*",
    "@kleavox/core": "workspace:*"
  },
  "devDependencies": {
    "@kleavox/typescript": "workspace:*",
    "@kleavox/vite-config": "workspace:*",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
