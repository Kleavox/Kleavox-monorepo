PUBLIC_ROOT_ORIGIN=https://example.com
PUBLIC_PORTFOLIO_ORIGIN=https://portfolio.example.com
# Production: real Turnstile site key (pair with `wrangler secret put TURNSTILE_SECRET_KEY` on the portfolio worker).
# Dev builds fall back to Cloudflare's public always-pass test key automatically.
PUBLIC_TURNSTILE_SITE_KEY=
