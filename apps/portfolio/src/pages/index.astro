---
import "../styles/global.css";

const disciplines = [
  ["embedded", "PCB design, ESP32 firmware, sensor integration"],
  ["software", "TypeScript, Go, Python, C and C++"],
  ["interfaces", "Astro, React, Flutter, accessible product UI"],
  ["systems", "Cloudflare, Linux, Docker, VPS operations"],
];

const projects = [
  {
    index: "01",
    name: "Zarkiv",
    status: "active",
    type: "edge ecosystem",
    description:
      "A Cloudflare-first ecosystem for identity, short routes, temporary file delivery, and infrastructure monitoring.",
    stack: "TypeScript / Workers / D1 / R2 / Go",
    href: "https://zarkiv.com",
  },
  {
    index: "02",
    name: "DeltaT32",
    status: "assembled",
    type: "embedded hardware",
    description:
      "A modular ESP32-C3 platform with hot-swappable BMI270 and MPU9250 sensor boards, designed in KiCad.",
    stack: "KiCad / ESP32-C3 / IMU / esp-rs",
    href: "https://github.com/Zarkir",
  },
  {
    index: "03",
    name: "Doswall",
    status: "shipped",
    type: "mobile system",
    description:
      "A Flutter rebuild of a lecturer wallboard application with live attendance, campus announcements, and geolocation.",
    stack: "Flutter / Dart / REST / Geolocation",
    href: "https://github.com/Zarkir",
  },
];

const timeline = [
  ["2022—now", "Computer Engineering, UNNES", "BSc program and thesis work"],
  ["2024 Q2", "Contract research staff", "Electoral data processing and analysis"],
  ["2024 Q4", "Dicoding", "Python, machine learning, and AI fundamentals"],
  ["2025 Q1", "Huawei HCIA-AI", "AI overview and HCIA-AI V3.5"],
  ["2025", "DeltaT32", "Modular PCB platform assembled"],
];
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta
      name="description"
      content="Portfolio of Hafidh Musyafa, a computer engineering student working across embedded systems, infrastructure, and product interfaces."
    />
    <meta name="theme-color" content="#080b10" />
    <meta property="og:title" content="Hafidh Musyafa | Systems and interfaces" />
    <meta
      property="og:description"
      content="Work spanning sensor hardware, edge infrastructure, and deliberate product interfaces."
    />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://port.zarkiv.com" />
    <link rel="canonical" href="https://port.zarkiv.com" />
    <title>Hafidh Musyafa | Systems and interfaces</title>
  </head>
  <body>
    <a class="skip-link" href="#main">Skip to content</a>
    <header class="port-nav">
      <a class="port-mark" href="/" aria-label="Portfolio home">
        <span>HM</span>
        <strong>PORT / 26</strong>
      </a>
      <nav aria-label="Portfolio navigation">
        <a href="#work">Work</a>
        <a href="#about">About</a>
        <a href="#contact">Contact</a>
        <a class="nav-system" href="https://zarkiv.com">Zarkiv ↗</a>
      </nav>
    </header>

    <main id="main">
      <section class="port-hero">
        <div class="hero-grid" aria-hidden="true"></div>
        <div class="hero-copy">
          <p class="mono-label">SEMARANG / INDONESIA / AVAILABLE</p>
          <h1>Hardware intuition.<br /><em>Software reach.</em></h1>
          <p class="hero-intro">
            I’m Hafidh Musyafa, a computer engineering student building across
            the entire signal path: from sensor boards and host agents to
            edge services and the interfaces people actually touch.
          </p>
          <div class="hero-actions">
            <a class="signal-action" href="#work">Inspect selected work <span>↓</span></a>
            <a class="quiet-action" href="https://github.com/Zarkir">GitHub ↗</a>
          </div>
        </div>

        <div class="instrument-stack" aria-label="Live engineering instruments">
          <article class="instrument instrument-primary">
            <header>
              <span><i></i> BMI270 / orientation</span>
              <b id="orientation-readout">X +0.00 / Y +0.00</b>
            </header>
            <div class="axis-stage" id="axis-stage">
              <span class="axis axis-x">X</span>
              <span class="axis axis-y">Y</span>
              <span class="axis axis-z">Z</span>
              <div class="board-model"></div>
            </div>
            <footer>I2C 0x68 <span>pointer reactive</span></footer>
          </article>
          <article class="instrument signal-panel">
            <header>
              <span><i></i> system / pulse</span>
              <b>64 BPM</b>
            </header>
            <svg viewBox="0 0 600 96" role="img" aria-label="Animated pulse signal">
              <path
                d="M0 64H70L82 61L92 66L104 62L119 64L135 58L148 12L162 82L176 51L194 64H270L282 61L292 66L304 62L319 64L335 58L348 12L362 82L376 51L394 64H470L482 61L492 66L504 62L519 64L535 58L548 12L562 82L576 51L600 64"
              ></path>
            </svg>
            <footer>250 HZ <span>signal nominal</span></footer>
          </article>
        </div>
      </section>

      <section class="manifesto">
        <p class="mono-label">OPERATING PRINCIPLE</p>
        <p>
          Build the physical thing early. Instrument it. Make the system
          readable. Then give the interface enough room to explain itself.
        </p>
      </section>

      <section id="work" class="port-section work-section">
        <header class="section-heading">
          <p class="mono-label">01 / SELECTED WORK</p>
          <h2>Systems with a pulse.</h2>
        </header>
        <div class="project-list">
          {
            projects.map((project) => (
              <a class="project-row reveal" href={project.href}>
                <div class="project-id">
                  <span>{project.index}</span>
                  <i data-status={project.status}></i>
                </div>
                <div class="project-name">
                  <h3>{project.name}</h3>
                  <p>{project.type}</p>
                </div>
                <p class="project-description">{project.description}</p>
                <div class="project-meta">
                  <span>{project.stack}</span>
                  <strong>{project.status} ↗</strong>
                </div>
              </a>
            ))
          }
        </div>
      </section>

      <section id="about" class="port-section about-section">
        <header class="section-heading">
          <p class="mono-label">02 / RANGE</p>
          <h2>One stack, many scales.</h2>
        </header>
        <div class="about-grid">
          <div class="discipline-list">
            {
              disciplines.map(([name, detail], index) => (
                <article class="reveal">
                  <span>0{index + 1}</span>
                  <h3>{name}</h3>
                  <p>{detail}</p>
                </article>
              ))
            }
          </div>
          <div class="timeline">
            <p class="mono-label">TIMELINE / SIGNAL LOG</p>
            <ol>
              {
                timeline.map(([date, title, note]) => (
                  <li>
                    <time>{date}</time>
                    <div>
                      <strong>{title}</strong>
                      <span>{note}</span>
                    </div>
                  </li>
                ))
              }
            </ol>
          </div>
        </div>
      </section>

      <section id="contact" class="contact-section">
        <p class="mono-label">03 / CONTACT</p>
        <h2>Have a system that needs a clearer signal?</h2>
        <div>
          <a class="signal-action" href="mailto:port@deau.site">
            port@deau.site <span>↗</span>
          </a>
          <p>Embedded systems, IoT, web products, or infrastructure work.</p>
        </div>
      </section>
    </main>

    <footer class="port-footer">
      <span>Hafidh Musyafa / {new Date().getFullYear()}</span>
      <span>Semarang, Indonesia</span>
      <div>
        <a href="https://github.com/Zarkir">GitHub</a>
        <a href="https://www.linkedin.com/in/hmus122/">LinkedIn</a>
      </div>
    </footer>

    <script>
      const stage = document.querySelector<HTMLElement>("#axis-stage");
      const model = document.querySelector<HTMLElement>(".board-model");
      const readout = document.querySelector<HTMLElement>("#orientation-readout");
      const reduced = window.matchMedia("(prefers-reduced-motion: reduce)").matches;

      if (stage && model && readout && !reduced) {
        stage.addEventListener("pointermove", (event: PointerEvent) => {
          const bounds = stage.getBoundingClientRect();
          const x = (event.clientX - bounds.left) / bounds.width - 0.5;
          const y = (event.clientY - bounds.top) / bounds.height - 0.5;
          model.style.transform =
            `rotateX(${(-y * 24).toFixed(2)}deg) rotateY(${(x * 34).toFixed(2)}deg)`;
          readout.textContent =
            `X ${(x * 2).toFixed(2)} / Y ${(-y * 2).toFixed(2)}`;
        });
        stage.addEventListener("pointerleave", () => {
          model.style.transform = "rotateX(58deg) rotateZ(-28deg)";
          readout.textContent = "X +0.00 / Y +0.00";
        });
      }

      if (!reduced && "IntersectionObserver" in window) {
        const observer = new IntersectionObserver(
          (entries) => {
            for (const entry of entries) {
              if (entry.isIntersecting) {
                entry.target.classList.add("is-visible");
                observer.unobserve(entry.target);
              }
            }
          },
          { threshold: 0.14 },
        );
        document.querySelectorAll(".reveal").forEach((item) => observer.observe(item));
      }
    </script>
  </body>
</html>
