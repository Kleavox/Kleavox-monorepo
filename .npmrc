# Suppress funding messages
fund=false

# unplugin-vue-router is a transitive dep of Nuxt 4 that carries a deprecation
# notice in its npm metadata for ALL versions (the package merged into vue-router).
# Since we cannot remove it without forking Nuxt itself, we suppress deprecation
# warnings at the npm output level. Errors still surface normally.
loglevel=error
