{
  "name": "@kleavox/link-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@kleavox/auth": "workspace:*",
    "@kleavox/config": "workspace:*",
    "@kleavox/core": "workspace:*",
    "@kleavox/crypto": "workspace:*",
    "@kleavox/link-app": "workspace:*",
    "@kleavox/link-protocol": "workspace:*",
    "@kleavox/worker": "workspace:*",
    "hono": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@kleavox/testing": "workspace:*",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  }
}
