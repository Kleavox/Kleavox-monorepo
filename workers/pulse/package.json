{
  "name": "@kleavox/pulse-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit --checkers 1"
  },
  "dependencies": {
    "@kleavox/auth": "workspace:*",
    "@kleavox/config": "workspace:*",
    "@kleavox/core": "workspace:*",
    "@kleavox/crypto": "workspace:*",
    "@kleavox/pulse-app": "workspace:*",
    "@kleavox/pulse-protocol": "workspace:*",
    "@kleavox/worker": "workspace:*",
    "hono": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@kleavox/typescript": "workspace:*",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  }
}
