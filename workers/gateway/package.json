{
  "name": "@kleavox/gateway-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit --checkers 1"
  },
  "dependencies": {
    "@kleavox/auth": "workspace:*",
    "@kleavox/config": "workspace:*",
    "@kleavox/core": "workspace:*",
    "@kleavox/web-app": "workspace:*",
    "hono": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@kleavox/typescript": "workspace:*",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  }
}
