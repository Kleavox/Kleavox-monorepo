name: Agent Release

on:
  push:
    tags:
      - "agent-v*"

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: services/agent
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: services/agent/go.mod
          cache-dependency-path: services/agent/go.mod
      - name: Test
        run: go test ./...
      - name: Build Linux binaries
        env:
          CGO_ENABLED: "0"
        run: |
          version="${GITHUB_REF_NAME#agent-v}"
          GOOS=linux GOARCH=amd64 go build -trimpath -ldflags="-s -w -X main.version=${version}" -o zarkiv-agent-linux-amd64 ./cmd/agent
          GOOS=linux GOARCH=arm64 go build -trimpath -ldflags="-s -w -X main.version=${version}" -o zarkiv-agent-linux-arm64 ./cmd/agent
          sha256sum zarkiv-agent-linux-amd64 > zarkiv-agent-linux-amd64.sha256
          sha256sum zarkiv-agent-linux-arm64 > zarkiv-agent-linux-arm64.sha256
      - name: Attest amd64 binary
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: services/agent/zarkiv-agent-linux-amd64
      - name: Attest arm64 binary
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: services/agent/zarkiv-agent-linux-arm64
      - name: Publish release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            services/agent/zarkiv-agent-linux-amd64
            services/agent/zarkiv-agent-linux-amd64.sha256
            services/agent/zarkiv-agent-linux-arm64
            services/agent/zarkiv-agent-linux-arm64.sha256
